const {Organization, Event, EventGroup, EventStaff, Attendee, EventTypes, OrganizationEventTypes, DefaultEventTypes} = require("../models");

async function createEvent(userId, name ){
    try {
        
    } catch (error) {
        
    }
}